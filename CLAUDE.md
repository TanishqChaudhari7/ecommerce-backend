# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is in its initial bootstrap phase — no application code, package manifest, or framework has been chosen yet. This file is a placeholder and should be rewritten as the project takes shape.

## Instructions for future updates

As soon as the project has real structure (a package manager file, a chosen framework/stack, a test runner, etc.), replace this file with:

1. **Commands** — how to install dependencies, run the dev server, run the test suite (including running a single test), lint, and build.
2. **Architecture** — the high-level shape of the backend (e.g., how routing/controllers/services/models are organized, how the database is accessed, auth strategy, any background job or queue system) — the kind of thing that requires reading several files to piece together.

Do not carry forward generic boilerplate ("write tests", "handle errors") — only document what is specific and non-obvious about this codebase.
